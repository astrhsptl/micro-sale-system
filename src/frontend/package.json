{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite ./src",
    "build": "tsc && vite build ./src/ --outDir ../build",
    "preview": "vite preview",
    "prod": "serve build -p 3000"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "path": "^0.12.7",
    "serve": "^14.2.1",
    "vite-tsconfig-paths": "^4.2.3"
  }
}
